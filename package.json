{
    "name": "smart-disaster-risk-prediction-system",
    "version": "1.0.0",
    "description": "Smart Disaster Risk Prediction System — monorepo runner",
    "private": true,
    "scripts": {
        "dev": "concurrently --kill-others-on-fail --prefix-colors \"bgBlue.bold,bgGreen.bold\" --names \"BACKEND,FRONTEND\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
        "install:all": "npm install --prefix backend && npm install --prefix frontend",
        "backend": "npm run dev --prefix backend",
        "frontend": "npm run dev --prefix frontend"
    },
    "dependencies": {
        "concurrently": "^10.0.3"
    }
}
